import React from "react";
// import { Row } from "reactstrap";

const Header = () => {
  return (
    <div className="header">
      <h1>Looking for a way to edit your PDF documents?</h1>
      <h3 className= 'sub-header'>
        We've got every tool you need to merge, split, compress, convert,
        rotate, unlock and watermark your PDFs with just a few clicks.
      </h3>
    </div>
  );
};

export default Header;
